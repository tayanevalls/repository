#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jsvalidate/bin/grunt-jsvalidate" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jsvalidate/bin/grunt-jsvalidate" "$@"
  ret=$?
fi
exit $ret
